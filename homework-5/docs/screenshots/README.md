# Screenshots

Capture one screenshot per configured MCP server, showing the **call** and its **result**,
and place them here with these exact names (referenced from the homework `README.md`):

| File | What it should show |
|------|---------------------|
| `github-mcp-result.png`       | A GitHub MCP call (e.g. list recent PRs / commits / create an issue) and its result. |
| `filesystem-mcp-result.png`   | A Filesystem MCP call (e.g. list files in `homework-5` or read a file) and its result. |
| `notion-mcp-result.png`       | The Notion MCP "last 5 bug pages" request and response — page IDs/titles only, no sensitive content. |
| `custom-mcp-read-tool-result.png` | The custom `lorem-ipsum` MCP `read` tool called with a `word_count` and its word-limited output. |
