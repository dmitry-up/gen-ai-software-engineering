# Screenshots

Capture the following evidence here and embed the same images in the PR description.

| File | What to capture |
|------|-----------------|
| `pipeline-run-py.png` | Full terminal output of `python integrator.py` |
| `test-coverage.png` | `python -m pytest` coverage report showing ≥ 90% |
| `skill-run-pipeline.png` | `/run-pipeline` skill executing in Claude Code |
| `hook-trigger.png` | Coverage-gate hook firing (or blocking a push) |
| `mcp-interaction.png` | context7 query result **and** a custom MCP tool call (e.g. `get_transaction_status`) |
